version: "2"
name: awsiamgroup
canonical: awsiamgroup
author: admin
status: private
description: awsiamgroup
image: ""
keywords:
- awsiamgroup
- provider:aws
technologies: []
dependencies: []
config:
  default:
    description: ""
    name: default
    pipeline:
      pipeline:
        path: pipeline/default/terraform-pipeline.yaml
      variables:
        destination: ($ project $)/pipeline/variables-($ environment $).yml
        path: pipeline/default/variables.sample.yaml
    terraform:
      awsiamgroup:
        destination: ($ project $)/terraform/($ environment $)/awsiamgroup.tf
        path: terraform/default/awsiamgroup.tf.sample
