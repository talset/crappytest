version: "2"
name: stackproject
canonical: stackproject
author: admin
status: private
description: stackproject
image: ""
keywords:
- stackproject
- provider:aws
technologies: []
dependencies: []
config:
  default:
    description: ""
    name: default
    pipeline:
      pipeline:
        path: pipeline/default/terraform-pipeline.yaml
      variables:
        destination: ($ project $)/pipeline/variables-($ environment $).yml
        path: pipeline/default/variables.sample.yaml
    terraform:
      stackproject:
        destination: ($ project $)/terraform/($ environment $)/stackproject.tf
        path: terraform/default/stackproject.tf.sample
