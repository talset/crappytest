version: "2"
name: newazure
canonical: newazure
author: admin
status: private
description: newazure
image: ""
keywords:
- newazure
- provider:azure
technologies: []
dependencies: []
config:
  default:
    description: ""
    name: default
    pipeline:
      pipeline:
        path: pipeline/default/terraform-pipeline.yaml
      variables:
        destination: ($ project $)/pipeline/variables-($ environment $).yml
        path: pipeline/default/variables.sample.yaml
    terraform:
      newazure:
        destination: ($ project $)/terraform/($ environment $)/newazure.tf
        path: terraform/default/newazure.tf.sample
