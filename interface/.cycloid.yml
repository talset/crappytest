version: "2"
name: interface
canonical: interface
author: admin
status: private
description: interface
image: ""
keywords:
- interface
- provider:azure
technologies: []
dependencies: []
config:
  default:
    name: default
    pipeline:
      pipeline:
        path: pipeline/default/terraform-pipeline.yaml
      variables:
        destination: ($ project $)/pipeline/variables-($ environment $).yml
        path: pipeline/default/variables.sample.yaml
    terraform:
      config:
        destination: ($ project $)/terraform/($ environment $)/config.tf
        path: terraform/default/config.tf.sample
      interface:
        destination: ($ project $)/terraform/($ environment $)/interface.tf
        path: terraform/default/interface.tf.sample
