version: "2"
name: project
canonical: project
author: admin
status: private
description: project
image: ""
keywords:
- project
- provider:aws
technologies: []
dependencies: []
config:
  default:
    description: ""
    name: default
    pipeline:
      pipeline:
        path: pipeline/default/terraform-pipeline.yaml
      variables:
        destination: ($ project $)/pipeline/variables-($ environment $).yml
        path: pipeline/default/variables.sample.yaml
    terraform:
      project:
        destination: ($ project $)/terraform/($ environment $)/project.tf
        path: terraform/default/project.tf.sample
