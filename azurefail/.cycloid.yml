version: "2"
name: azurefail
canonical: azurefail
author: admin
status: private
description: azurefail
image: ""
keywords:
- azurefail
- provider:azure
technologies: []
dependencies: []
config:
  default:
    description: ""
    name: default
    pipeline:
      pipeline:
        path: pipeline/default/terraform-pipeline.yaml
      variables:
        destination: ($ project $)/pipeline/variables-($ environment $).yml
        path: pipeline/default/variables.sample.yaml
    terraform:
      azurefail:
        destination: ($ project $)/terraform/($ environment $)/azurefail.tf
        path: terraform/default/azurefail.tf.sample
