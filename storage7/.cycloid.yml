version: "2"
name: storage7
canonical: storage7
author: admin
status: private
description: storage7
image: ""
keywords:
- storage7
- provider:azure
technologies: []
dependencies: []
config:
  default:
    name: default
    pipeline:
      pipeline:
        path: pipeline/default/terraform-pipeline.yaml
      variables:
        destination: ($ project $)/pipeline/variables-($ environment $).yml
        path: pipeline/default/variables.sample.yaml
    terraform:
      config:
        destination: ($ project $)/terraform/($ environment $)/config.tf
        path: terraform/default/config.tf.sample
      storage7:
        destination: ($ project $)/terraform/($ environment $)/storage7.tf
        path: terraform/default/storage7.tf.sample
